# RB Harness Init — Canonical Post-Phase-3.5 Checkpoint

<!-- authoritative-continuation-checkpoint: post-phase-3.5-canonical-init-cutover -->

Checkpoint date: 2026-08-29 (America/Sao_Paulo)
Recovery-budget continuation refresh: 2026-08-30 (America/Sao_Paulo)

This document is the authoritative continuation checkpoint for work after the
Phase 3.5 freeze and the canonical Init cutover. If an older checkpoint or
architecture/specification document conflicts with this document about the
post-Phase-3.5 repository state, this document wins. Older documents remain
historical records and must not be rewritten or deleted.

Evidence labels used here:

- **OBSERVED** means current source, tests, generated bytes, Git, or an offline
  command directly proves the statement.
- **CONFIRMED CONTINUATION CONTEXT** means the operator supplied the historical
  result for this checkpoint, but the complete raw run is not necessarily
  committed in this repository.
- **PLANNED / NOT IMPLEMENTED** means the item is a future decision and must not
  be mistaken for current behavior.

No provider or model was called while producing this checkpoint. Verification
used local fixtures and offline conformance-record replay only.

## 1. Repository and Git state

### 1.1 Frozen committed Phase 3.5

- Branch tip/HEAD at checkpoint start:
  `0bbe610f623c6417d30ed2d57f019bb95dba2404`.
- Frozen Phase 3.5 commit:
  `0bbe610f623c6417d30ed2d57f019bb95dba2404`.
- Commit message: `feat(vnext): complete semantic init phase 3.5`.
- Therefore HEAD contains the frozen, committed Phase 3.5 implementation and no
  later commit.

### 1.2 Canonical Init cutover, recovery freeze, and current continuation

Current continuation state as of the 2026-08-30 Progressive Init Phase 2 work:

- Frozen Phase 3.5 commit:
  `0bbe610f623c6417d30ed2d57f019bb95dba2404`.
- Canonical Init cutover branch: `feat/init-cutover-wizard`.
- Canonical Init cutover commit:
  `744305e52012462770b2a9815fc8821a64102954`, whose parent is the frozen Phase
  3.5 commit.
- The canonical Init cutover was committed and pushed before the dedicated
  recovery-budget branch was created.
- Recovery-budget branch: `feat/recovery-budget-2-3-5-7`.
- Frozen and pushed recovery-budget commit:
  `5a3481ee74f00c8a7e1805f49330c1220803505a`, whose parent is the canonical
  Init cutover commit.
- Current Progressive Init foundation branch: `feat/progressive-init-foundation`.
- Frozen Progressive Init foundation commit:
  `bb90ab81d9d3580c571035e3e36399ad458b0c4a`, whose parent is the frozen
  recovery-budget commit.
- Progressive Init Phase 2 branch: `feat/progressive-init-user-stories`, based
  exactly on the frozen foundation commit.
- Progressive Init Phase 2 user-stories is implemented as uncommitted worktree
  changes on that dedicated branch. No Phase 2 implementation commit hash
  exists.
- The source/package/plugin/runtime version remains `0.6.2`; the recovery
  change does not include a release/version bump.

**Historical cutover-preparation context:** the original 2026-08-29 checkpoint
was produced while `feat/init-cutover-wizard` still pointed at the Phase 3.5
freeze and the canonical Init cutover existed as uncommitted worktree changes.
The historical statement that no commit or push had been performed applies
only to that original checkpoint moment, not to the current continuation state.
Before the cutover commit, an incomplete unauthorized `1.0.0` sweep was
inspected and excluded: files whose only diff was that sweep were restored to
their `0.6.2` state, while legitimate canonical-Init documentation changes were
preserved.

**Historical worktree context:** at the original checkpoint time, the
project-local untracked `AGENTS.md` directed work to `resgate`, which was
checked out in a separate clean worktree and divergent from the cutover branch.
The original checkpoint remained beside the then-uncommitted cutover state
without discarding or rewriting either worktree. This remains provenance for
that historical snapshot; current unrelated `package.json` and `AGENTS.md`
ownership is recorded separately in section 1.3.

### 1.3 Pre-existing unrelated user worktree state

The operator identifies these as pre-existing unrelated user work and they must
not be absorbed, reverted, or attributed to the cutover without a separate
inspection:

- `package.json` (modified);
- `AGENTS.md` (untracked).

Although the current root `package.json` contains version `1.0.0` and an
`allowScripts.esbuild` entry, ownership remains classified as user-supplied for
this checkpoint as explicitly instructed.

### 1.4 Exact resolved status

The following is the exact `git status --short` after version-family cleanup,
deterministic regeneration, final verification, and checkpoint refresh:

```text
 M docs/funcionalidades.pt-BR.md
 M docs/how-to-use.md
 M package.json
 M packages/core/src/cli-program.ts
 M packages/core/src/harness-dashboard.ts
 M packages/core/src/harness-splash.ts
 M packages/core/src/harness-wizard.ts
 M packages/core/src/vnext/init-cli.ts
 M packages/core/src/vnext/init.ts
 M packages/core/test/cli-compatibility.test.ts
 M packages/core/test/fixtures/cli-surface.json
 M packages/core/test/headless-init.test.ts
 M packages/core/test/standalone.test.ts
 M packages/core/test/vnext/phase3-init.test.ts
 M packages/core/test/vnext/providers/boundary.test.ts
 M plugins/rb-harness/scripts/rb-harness.cjs
?? AGENTS.md
?? docs/RB-HARNESS-CANONICAL-INIT-CHECKPOINT.md
?? packages/core/src/harness-mascot.ts
?? packages/core/src/init-routing.ts
?? packages/core/src/init-wizard.ts
?? packages/core/src/root-wizard.ts
?? packages/core/test/init-cutover.test.ts
?? packages/core/test/init-dashboard.test.ts
```

### 1.5 Generated bundle state

`plugins/rb-harness/scripts/rb-harness.cjs` is modified relative to the frozen
commit. A clean `npm run build` deterministically reproduced its current
SHA-256 as:

```text
41a78d72bc689e1a18b64f912a2fa4cc29ebcc9b5cfeec7a239877355fe84ed2
```

Two clean builds produced byte-identical output. The actual
repository/source-derived digest wins; do not restore the bundle by historical
hash preference. The generated `packages/core/dist/headless-init-bundle.json`
and `headless-interview-bundle.json` both report version `0.6.2` and pin the
actual `41a78d72...` digest after build.

## 2. Product terminology

The product-facing name is **RB Harness Init**. Do not call the public product
“vNext Init.” The canonical Init engine is the formerly validated vNext
semantic engine promoted to the public `init` command.

The old term `vnext` intentionally remains in internal source paths, persisted
run-state format/path names, test directories, internal compatibility aliases,
and conformance laboratory commands. There is no product need to rename those
directories cosmetically. Rename only if a later functional migration actually
requires it.

Current product boundary:

- public `rb-harness vnext init` is removed;
- during Progressive construction, public bare `rb-harness init` continues to
  own canonical semantic Init;
- only explicit `rb-harness init --stage <stage>` (including the equivalent
  root-selector form) enters Progressive focused execution;
- the former legacy semantic Init planner is retired from public Init;
- the independently published headless contracts remain supported;
- `vnext conformance` remains an internal/laboratory name.

Evidence: `packages/core/src/cli-program.ts`,
`packages/core/src/vnext/init-cli.ts`,
`packages/core/test/vnext/providers/boundary.test.ts`, and
`packages/core/test/cli-compatibility.test.ts`.

## 3. Canonical CLI topology

### 3.1 Intended entry points

```text
rb-harness
  -> interactive root wizard

rb-harness --init
  -> interactive Init configuration wizard

rb-harness --init --dashboard
  -> interactive Init configuration wizard, then dashboard presentation

rb-harness init ...
  -> direct canonical semantic Init configuration, without the configuration wizard
```

`rb-harness wizard` still exists as an explicit compatibility entry point to
the root product shell, but no `--wizard` flag is required: bare `rb-harness`
is the default product interface in a TTY.

### 3.2 Deterministic routing categories

Root operation selectors/special actions currently exposed by source are:

- `--init`: select Init; selector-only in a TTY opens the Init configuration
  wizard;
- `--login`: launch the guided direct-API credential configuration action;
- `--splash`: play the canonical splash and exit;
- `--ver`/`--version`: informational version actions.

Other workflows use subcommands (`ai-context`, `plan`, `evolve`, `review`) or
are selected inside the root wizard; there are no corresponding root selector
flags for them.

Presentation modifiers used by root/Init routing are:

- `--dashboard`;
- `--no-splash`.

Presentation modifiers alone do not select direct Init. In particular,
`rb-harness --dashboard` in a TTY opens the root wizard with dashboard intent.

The canonical Init operational inputs are exactly:

- positional `[request...]`;
- `--profile <profile-id>`;
- `--file <path>`;
- `--credential <id-or-label>`;
- `--project <path>` (default `.`);
- `--headless`;
- `--timeout <seconds>` (default `120`, valid `1..900`);
- `--dashboard` as its presentation option.

Progressive Phase 1 adds `--stage <stage>` as an explicit focused-operation
selector. Its presence is the only Phase-1 routing discriminator. Repository
state, `.spec/init` existence, request content, argument order, profile, TTY,
and freshness never switch engines. `--stage` cannot be combined with
`--dashboard` until a truthful Progressive dashboard exists; that combination
fails deterministically before semantic execution.

For `--init` routing, `--project`, `--profile`, `--credential`, `--file`,
`--headless`, `--timeout`, help, or positional request text imply direct
configuration mode. `--profile` and either request text or `--file` are
required before direct execution; an incomplete direct command fails with an
exact missing-input list and points back to `rb-harness --init`.

The Init configuration wizard and the semantic interview are separate layers:

- the configuration wizard chooses project, exact profile, request source,
  optional direct-API credential, dashboard, and whether to execute;
- the semantic interview resolves material product/architecture ambiguity
  after the intent semantic operation.

Direct CLI mode bypasses only the configuration wizard. In an interactive TTY
it still performs the semantic interview unless `--headless` is supplied. A
non-TTY direct invocation also uses the non-interactive recommendation policy,
even without an explicit `--headless`, because no answer channel exists.

Selector-only root or Init invocation without a TTY never opens readline and
never hangs: routing prints help and fails with deterministic guidance. Source
authority: `packages/core/src/init-routing.ts`; regression authority:
`packages/core/test/init-cutover.test.ts`.

## 4. Root wizard

The root wizard is above Init and is the default interactive RB Harness product
interface. `ROOT_OPERATIONS` in `packages/core/src/root-wizard.ts` lists exactly
these real operations, in this order:

1. `init` — create a Ralph plan for a new project;
2. `ai-context` — map the AS IS state of an implemented project;
3. `plan` — plan an isolated change;
4. `evolve` — evolve existing behavior safely;
5. `review` — audit the product.

Selecting `init` dispatches to the canonical Init configuration wizard.
Selecting any other operation delegates to the existing standalone wizard with
the selected workflow preserved; those workflows retain their current
implementations and were not cut over to the canonical Init engine.

## 5. Canonical Init engine and call chain

The direct public call chain is:

```text
packages/core/src/cli.ts
  runHarnessCli()
    -> classifyRootCliArgs() when root selectors are present
    -> Commander `init` action
    -> no `--stage`: runCanonicalInit()
    -> runInitCommand()
    -> runSemanticInit()
    -> SemanticGateway.generate("intent")
    -> interview selection and Core authority resolution
    -> SemanticGateway.generate("work")
    -> closeInitProject()
    -> publishStagedRb()
```

The temporary Phase-1 sibling branch is deliberately narrow:

```text
Commander `init` action
  -> explicit `--stage`: runProgressiveInitCommand() -> runProgressiveInit()
  -> otherwise: exact canonical chain above
```

Wizard paths converge before semantic execution:

```text
runRootWizard() -> runInitWizard() -> runCanonicalInit() -> runInitCommand()
runInitWizard() --------------------^
```

Exact modules/functions:

- `packages/core/src/cli-program.ts`: `runHarnessCli()`,
  `runCanonicalRootWizard()`, `runCanonicalInitWizard()`, and
  `runCanonicalInit()`;
- `packages/core/src/init-routing.ts`: deterministic root/Init classification;
- `packages/core/src/root-wizard.ts`: product operation selection/dispatch;
- `packages/core/src/init-wizard.ts`: Init configuration collection;
- `packages/core/src/vnext/init-cli.ts`: `runInitCommand()` and profile/auth/
  interview-channel setup;
- `packages/core/src/vnext/init.ts`: `runSemanticInit()` orchestration;
- `packages/core/src/vnext/gateway.ts`: bounded semantic operations and
  transport calls;
- `packages/core/src/vnext/closure.ts`: deterministic semantic closure;
- `packages/core/src/vnext/publish.ts`: atomic `.rb` publication with
  filesystem-fault rollback.

`runVnextInitCommand` remains only an internal compatibility export alias for
`runInitCommand`; no public `vnext init` Commander route exists.

No legacy semantic planner or automatic fallback is reachable from canonical
Init. The generic standalone planner remains reachable only for the other
legitimate workflows. `resume` explicitly rejects old standalone runs whose
workflow was `init` and instructs the operator to start canonical Init.

Primary modules still under `packages/core/src/vnext/` are:

- orchestration: `init-cli.ts`, `init.ts`, `gateway.ts`, `run-state.ts`;
- semantic input/authority: `prompts.ts`, `interview.ts`, `provenance.ts`,
  `wire.ts`;
- workflow IR/Core: `ir.ts`, `identity.ts`, `task-contract.ts`,
  `path-contract.ts`, `resolve.ts`, `validate.ts`, `result.ts`;
- recovery evidence: `recovery-findings.ts`, `rejected-evidence.ts`;
- closure/fidelity/publication: `closure.ts`, `ralph-fidelity.ts`,
  `publish.ts`, and `render/{brief,execution,manifest}.ts`;
- provider boundary: `providers/contract.ts`, `providers/registry.ts`, the
  Anthropic direct/Claude Code adapters, and conformance modules/records.

## 6. Semantic pipeline

The dashboard projects the canonical pipeline as:

```text
pedido -> intent -> entrevista -> decisões -> work -> closure
```

These labels combine semantic operations with orchestration/presentation
stages:

| Dashboard stage | Run-state stages | Nature |
|---|---|---|
| `pedido` | `request-received` | orchestration: bind request/profile/run evidence |
| `intent` | `intent-requested`, `intent-decoded` | **semantic operation**: generate/decode intent IR |
| `entrevista` | `interview-pending` | orchestration plus user/policy authority selection |
| `decisões` | `intent-resolved` | deterministic resolution of request/interview authority |
| `work` | `work-requested`, `work-resolved` | **semantic operation**: generate/decode work IR |
| `closure` | `deterministic-closure`, `published` | deterministic Core validation, rendering, manifest closure, and publication |

A healthy no-correction run therefore has two semantic operations: intent and
work. Interview/resolved authority is not a third model call. Closure performs
zero provider, adapter, formatter, repair, or provider-specific calls.

## 7. Interview model

Every material proposed question has:

- a stable semantic key and concrete question;
- one concrete selectable `Recommended` value;
- a useful `Why`/rationale;
- zero or more concrete alternatives.

`formatInteractiveQuestion()` renders those four parts and explicitly says a
blank answer accepts the recommendation.

Selection behavior:

- nonblank interactive response -> `explicit` acceptance and `user-answer`
  authority;
- blank interactive response -> exact recommended value,
  `blank-interactive`, and `accepted-recommendation` authority;
- headless/non-interactive policy -> exact recommended value,
  `non-interactive-policy`, and `accepted-recommendation` authority.

Accepted recommendation is a distinct, Core-verifiable provenance kind. It may
resolve a RIGID product or architecture decision because the recommendation was
materialized as a question before selection. A vague recommendation such as
“use a suitable database” is rejected.

Silent model defaults cannot establish RIGID product/architecture authority.
Core emits I-17 for a `model-default` that tries to decide such a determination.

Question count is not fixed. The intent slice emits the set of material
questions, every one is persisted before selection, and execution proceeds
only after all emitted questions resolve and `ensureIntentSufficient()` accepts
the intent. The current interview is one intent-generated batch traversed
question by question; it is not the older standalone adaptive-round engine.

Evidence: `packages/core/src/vnext/interview.ts`,
`packages/core/src/vnext/init.ts`, and interview regressions in
`packages/core/test/vnext/phase3-init.test.ts`.

## 8. Provenance and authority

Current authored determination-source kinds in `packages/core/src/vnext/ir.ts`
are:

- `request` with an evidence phrase;
- `user-answer` with a question key;
- `accepted-recommendation` with a question key;
- `model-default`.

Protected paths support `built-in`, `request`, `user-answer`, and
`accepted-recommendation` after resolution.

Authority semantics:

- original request authority requires a meaningful contiguous request phrase;
  short incidental substrings do not prove a decision;
- explicit user answers are stored by semantic question key and verified as
  nonempty;
- accepted recommendations retain the exact selected value plus
  `blank-interactive` or `non-interactive-policy` proof;
- resolved interview authority is projected into `selectedDecisions` for the
  work request and into Core provenance/determinations;
- accepted-recommendation determinations/protected paths must equal the
  verified selected value.

Progressive project-description adds a stricter live-to-persisted boundary.
Provider/model output cannot author `developer` authority. Live request and
interview authority is independently verified by Core before acceptance;
Progressive request authority requires a meaningful contiguous request span
selected by the model, while Core derives the authority-bearing request fact
from that verified evidence. Core does not infer entailment between a free-form
model statement and request evidence; interpretations beyond the request text
require another legitimate authority source. This structural binding is
Progressive-specific: canonical Init request provenance remains at its frozen
pre-Progressive behavior. Live dogfood replaced the earlier exact
statement/evidence wire contract because natural supported-model output does
not reliably repeat the same request fact as two character-equivalent authored
fields.

Once a valid live project description crosses the persistence boundary,
`.spec/init/project-description.md` renders every determination as `developer`
authority because the document is developer-owned semantic state for future
runs. Its strict parser honors only `developer` determination sources. Editable
Markdown therefore cannot manufacture request, `user-answer`,
`accepted-recommendation`, or `model-default` history. `.rb-harness` metadata is
never used to restore or manufacture authority.

`VnextInitRunState`, question history, attempts, counters, recovery audits, and
rejected snippets are orchestration/debug evidence only. They are not fields of
`InitProjectModel` and are never a substitute for request/user/accepted-
recommendation authority.

## 9. Workflow IR and deterministic Core

The architecture is deliberately:

```text
workflow-specific IR -> shared deterministic primitives/Core -> exact renderer/closure
```

It is not a universal mega `ProjectModel` full of optional fields. Current Init
uses `SemanticInitProject`/`InitProjectModel`; future workflows/stages should
have their own IRs over shared primitives.

Stable/core-owned concepts and authorities include:

- code-owned project, requirement, phase, task, acceptance, and artifact IDs:
  `packages/core/src/vnext/identity.ts`;
- model-authored symbolic references (`SemanticKey`) with one shared grammar:
  `identity.ts`;
- shared task required fields/cardinality:
  `packages/core/src/vnext/task-contract.ts`;
- shared project-relative ownership grammar:
  `packages/core/src/vnext/path-contract.ts`;
- workflow IR and closed validation-intent union:
  `packages/core/src/vnext/ir.ts`;
- deterministic ID assignment, reference resolution, built-in protected paths,
  and `parallelSafe: false`: `packages/core/src/vnext/resolve.ts`;
- canonicalization and invariants I-01..I-20:
  `packages/core/src/vnext/validate.ts`;
- provider schema and strict wire decoding:
  `packages/core/src/vnext/wire.ts`;
- exhaustive Ralph parser/Core fidelity:
  `packages/core/src/vnext/ralph-fidelity.ts`;
- exact BRIEF/PHASES/manifest renderers and closure:
  `packages/core/src/vnext/render/` and `closure.ts`.

`INIT_PROJECT_IR_CONSUMERS` provides a compile-time consumer witness for every
current IR field, so adding a field requires a present-tense consumer.

Canonicalization normalizes presentation variance (text spacing, path
separators/trailing slashes for Core inputs, sort/dedup of relevant references)
but does not invent missing semantics. Deterministic validation closes before
publication, then Ralph round-trip, manifest validation, tree validation,
exactly-one-READY-plan selection, and post-publication hash verification close
the output.

## 10. Task contract

Every semantic task requires all of these fields:

- `intent`;
- `ownedPaths`;
- `covers`;
- `acceptance`;
- `validation`;
- `expectedEvidence`.

`key`, `title`, and `dependsOn` are also part of the wire task. Core later adds
the code-owned task ID, acceptance IDs, resolved requirement/task references,
and literal `parallelSafe: false`.

The acceptance ceiling is:

```ts
TASK_ACCEPTANCE_MAX_ITEMS = 6
```

Its single source authority is
`packages/core/src/vnext/task-contract.ts`. The provider schema imports it as
`maxItems`, the wire decoder rejects more than six, and Core validation uses
the same constant. There is no truncation or repair. An over-limit candidate
is rejected as a whole and may consume a bounded whole-slice corrective
regeneration.

## 11. SemanticKey contract

The exact pattern is:

```text
^[a-z][a-z0-9-]{1,47}$
```

`SEMANTIC_KEY_PATTERN` in `packages/core/src/vnext/identity.ts` is the shared
authority. Provider schemas import it, wire decoding calls `semanticKey()`,
resolution validates declarations/references, and Core validates canonical
models against the same grammar.

Keys are model-authored semantic references, not machine execution IDs. Core
derives `R-001`, `P01`, `T001`, `AC-T001-01`, and artifact IDs after semantic
resolution.

## 12. `ownedPaths` contract

The exact shared pattern authority is
`PROJECT_RELATIVE_PATH_PATTERN` in
`packages/core/src/vnext/path-contract.ts`:

```text
^(?![A-Za-z]:/)(?!(?:\*|\*\*|\*\*/\*)$)(?:(?!\.{1,2}(?:/|$))[^/\\\u0000\n\r\t`]+)(?:/(?:(?!\.{1,2}(?:/|$))[^/\\\u0000\n\r\t`]+))*$
```

Supported forms are:

- an explicit project-relative file, for example `package.json` or
  `src/domain/service.ts`;
- a project-relative directory token without a trailing `/`, for example
  `migrations` or `src/domain`; directory ownership covers its subtree;
- a bounded glob below a project-relative root, for example `src/**/*.ts`.

Rejected forms include:

- absolute paths and Windows drive roots;
- backslashes;
- empty, `.` or `..` segments;
- NUL, newline, carriage return, tab, or backtick;
- trailing `/` directory syntax;
- project-wide unbounded `*`, `**`, or `**/*`.

Independent Core rules also reject intersections with `.rb`, `.rb-harness`,
`.git`, the narrowly protected developer-owned `.spec/init` subtree, and any
authoritative protected path. Unrelated `.spec` paths remain eligible for task
ownership. Provider schema, work decoder, Core, and the existing execution-plan
protected-path intersection machinery fail closed. Current regressions
prove valid `migrations`, `src/domain/destinations`, `tests`, `src/**/*.ts`, and
`package.json`, and reject their trailing-slash forms plus unsafe traversal and
global globs.

## 13. Quality commands and validation intents

First-pass quality commands must:

- be an actual one-line executable command;
- run non-interactively;
- terminate by themselves;
- return their real exit status;
- not mask failure with `|| true`, `; true`, or forced `exit 0`.

They must not be development/application servers, watchers, long-running
workers, or interactive processes. The prompt and deterministic validator
explicitly reject representative server/watch commands and flags. The
`QualityCommandKind` value `run` does not make a long-running command valid.

Current validation kinds are a closed union:

- `command`: executor runs an exact declared quality-command key and observes
  completion/exit status;
- `manual`: executor performs a non-command inspection or action; executable
  shell prose disguised as manual is rejected;
- `human`: evidence genuinely requires human judgement/intervention unavailable
  to the executor.

Provider wire uses `{kind, value}` and Core resolves it to the typed variants in
`ir.ts`. Rendering emits backticked commands, `manual: ...`, or `human: ...`.

## 14. Visual acceptance and I-13

Final invariant: I-13 positively identifies unsupported explicit visual or
aesthetic semantics. Neutral UI vocabulary is not inherently visual.

Functional UI acceptance is valid when it states deterministic behavior,
state, or data, including:

- application or DOM state;
- navigation;
- form submission;
- an exact count, value, or message;
- filtering or sorting;
- authorization-dependent enabled/disabled action state;
- an API/HTTP effect observable through UI behavior;
- deterministic state transitions.

Unsupported explicit visual semantics remain invalid as canonical Init
acceptance boundaries:

- styling and colors;
- spacing;
- visual prominence or distinction;
- positioning/alignment as appearance;
- screenshot/pixel comparison;
- responsive/geometry/rendered-motion proof outside the current semantic
  model;
- visual fidelity and aesthetic/layout judgement.

Mixed functional plus explicit aesthetic acceptance remains invalid. Genuine
visual judgement may be separated into a `human` validation while acceptance
stays functional.

At the Phase 3.5 freeze, the old language-sensitive
`STRONG_VISUAL_CRITERION`/`UI_SURFACE`/weak-verb rescue architecture was removed.
It had produced false positives from neutral words such as screen, panel,
display, render, visible, and hidden. The current
`AESTHETIC_VISUAL_BOUNDARY` plus
`UNSUPPORTED_RENDERED_VISUAL_BOUNDARY` in
`packages/core/src/execution-contract.ts` identifies the unsupported semantics
instead of trying to whitelist functional language.

Real purchasing-flow dogfood supplied the Portuguese regressions now in
`packages/core/test/execution-contract.test.ts` and
`packages/core/test/vnext/validation.test.ts`: counts, 403/list-empty behavior,
self-approval disabled state, filtered navigation/listing, dashboard exact
counts, and rejection/correction state transitions do not emit I-13. Explicit
layout/visibility and “visually prominent in red” still do.

## 15. Current recovery architecture and budgets

The current production values are:

| Budget | Current value |
|---|---:|
| corrective regenerations per slice | 2 |
| corrective regenerations per run | 3 |
| semantic operations per run | 5 |
| transport invocations per run | 7 |
| transport retries per semantic operation | 1 |
| transport retries per run | 2 |

`CANONICAL_INIT_RECOVERY_BUDGET` in
`packages/core/src/vnext/recovery-budget.ts` is the single production authority
for all six limits. `packages/core/src/vnext/gateway.ts` consumes that authority
for semantic corrections, operations, transport invocations, and transport
retries. There is no public recovery-budget option.

Recovery semantics:

- regenerate one complete intent or work slice;
- apply every violated rule globally across the regenerated slice;
- treat prior pointers as evidence from the rejected attempt, not patch
  targets;
- bind the authoritative original request and, for work, the complete resolved
  intent/interview authority;
- never partially patch a provider candidate;
- never run a JSON formatter/fixer;
- never repair Markdown;
- never splice documents;
- never fall back to another provider/profile/transport.

Two complete corrective regenerations in the same slice are allowed. A third
correction in that slice is forbidden. Across the Init run, a fourth correction
is forbidden even when a slice still has unused local capacity. Transport
retries remain independent from semantic corrections and retain their exact
limits of one retry per semantic operation and two retries per run. No provider,
profile, model, or transport fallback was added.

Under this envelope, the transport-invocation ceiling is fail-closed defense in
depth rather than the first binding guard: Harness-owned invocations cannot
exceed semantic operations plus transport retries (`5 + 2 = 7`). The semantic-
operation or retry guard therefore terminates an attempted expansion before an
eighth invocation.

The canonical semantic engine may canonicalize an already decoded Core model,
but it never repairs missing/invalid model semantics. Recovery is another
bounded semantic operation for the whole slice.

## 16. Implemented bounded recovery envelope

The recovery-budget branch implements this bounded increase over the canonical
Init cutover base:

| Budget | From | To |
|---|---:|---:|
| corrections per slice | 1 | 2 |
| corrections per run | 2 | 3 |
| semantic operations per run | 4 | 5 |
| transport invocations per run | 6 | 7 |

Transport retry budgets remain unchanged:

- retry per semantic operation: 1;
- retries per run: 2.

Rationale: after deterministic Harness defects were remediated, recovery is now
bounded tolerance for model error. No public `--recovery-budget` knob exists.

Maximum intended healthy envelope:

```text
intent original
intent correction
work original
work correction 1
work correction 2
= 5 semantic operations / 3 corrective regenerations
```

Deterministic scripted-adapter regressions cover zero, one, two, and three total
corrections; two corrections in the same slice; the maximum healthy envelope;
per-slice, global, semantic-operation, and transport bounds; second-correction
evidence; and dashboard rendering of three corrections. Whole-slice recovery,
run-state authority isolation, rejected-candidate evidence, retry independence,
fail-closed publication, and provider neutrality remain unchanged.

## 17. Recovery rule ledger

`RecoveryRule` in `packages/core/src/vnext/recovery-findings.ts` is the current
closed vocabulary:

1. `acceptance-no-visual-only`;
2. `acceptance-self-contained`;
3. `requirement-coverage`;
4. `validation-command-executable`;
5. `validation-command-terminating`;
6. `task-completeness`;
7. `rigid-decision-requires-authority`;
8. `semantic-key-valid`;
9. `dependency-order-valid`.

Rules are derived from provider-neutral wire/Core findings. A `Set` preserves
first-finding order and deterministically deduplicates rules. Unknown findings
do not fabricate a global rule; they retain sanitized specific guidance only.
Code-owned IDs (`R-001`, `P01`, `T001`, acceptance IDs, artifact IDs) are
excluded from model-facing guidance and unsafe identities fail closed to
generic Core guidance.

All rules in a correction apply to the whole regenerated slice. Specific
findings are diagnostic context, never localized patch instructions.

Remaining minor technical debt: `recoveryRule()` still maps several rules by
matching current finding message prose/substrings plus pointers. The vocabulary
is closed and tested, but this mapping is not yet entirely code-based finding
metadata. Do not expand it casually; a future cleanup should add typed finding
codes without changing model-facing semantics.

## 18. Recovery observability

`RecoveryAttemptEvidence` is defined in
`packages/core/src/vnext/run-state.ts` and persisted inside:

```text
.rb-harness/runs/<runId>/vnext-init-state.json
```

Each corrective attempt records:

- semantic `slice` (`intent` or `work`);
- semantic-operation `ordinal`;
- recovery scope flags proving complete-slice regeneration, global rule
  application, and previous-pointer evidence;
- deduplicated violated rules;
- sanitized specific prior findings (`pointer` plus model-facing guidance);
- SHA-256 of the original request;
- SHA-256 of authoritative input;
- for work, SHA-256 of resolved interview authority;
- SHA-256 of the recovery context;
- SHA-256 of the exact corrective input.

`packages/core/src/vnext/prompts.ts` constructs both the actual model-facing
corrective JSON string and its audit evidence from the same recovery context,
then hashes the exact string. This binds observability to what the adapter
actually received.

This state is audit/debug evidence only. It is never semantic authority and is
outside `.rb`. It stores no raw provider envelope and no credential/secret.

## 19. Rejected-candidate observability

`RejectedFindingEvidence` in
`packages/core/src/vnext/rejected-evidence.ts` stores only allowlisted narrow
evidence for rejected candidates:

- safe pointer;
- recovery rule where one is known;
- a narrow rejected scalar/typed validation value when allowlisted;
- SHA-256 of the sanitized value;
- `valueTruncated: true` when applicable;
- structural `observed.count` for collection-cardinality findings.

Current limits are:

```text
REJECTED_EVIDENCE_STRING_LIMIT = 512 characters
REJECTED_EVIDENCE_ENTRY_LIMIT  = 64 entries
```

Sanitization redacts authorization/API-key headers, bearer tokens,
`sk-ant-*`, secret-like environment assignments, and `/home/<identity>/`.
Strings are bounded after redaction; hashes bind the sanitized full value.

Accepted attempts do not receive rejected dumps. Rejected evidence is only
written into run-state attempt evidence and has no consumer in semantic
resolution, recovery input, rendering, closure, or publication.

## 20. Provider support

Only two exact profiles are registered in the canonical Init registry.

### 20.1 Anthropic direct API + Claude Opus 5

- Profile ID: `anthropic:claude-opus-5`.
- Status from current verified record: **SUPPORTED**.
- Transport: `direct-api`.
- `requestAccounting`: `exact`.
- Structured output: forced tool argument.
- Model ID: `claude-opus-5`.
- Current offline record replay: **16/16 passed**.
- Record:
  `packages/core/src/vnext/providers/conformance/records/anthropic_claude-opus-5.json`.

### 20.2 Claude Code CLI + Claude Opus 5

- Profile ID: `anthropic:claude-code-cli:claude-opus-5`.
- Status from current verified record: **SUPPORTED**.
- Transport: `claude-code-cli`.
- `requestAccounting`: `opaque`.
- One Harness adapter request owns one isolated CLI process/transport
  invocation; provider-internal model requests are not authoritatively
  measurable.
- Model ID: `claude-opus-5`.
- Exact recorded transport version: `2.1.251 (Claude Code)`; runtime policy is
  `exact-recorded` and preflight rejects version drift before semantic use.
- Current offline record replay: **26/26 passed**.
- Record:
  `packages/core/src/vnext/providers/conformance/records/anthropic_claude-code-cli_claude-opus-5.json`.

Current Claude Code invocation policy is code-owned in
`packages/core/src/vnext/providers/anthropic/claude-code/process.ts`:

- effort `low`, `maxTurns: 6`;
- structured-output retry limit `5` inside the CLI;
- transport retry limit `0` inside the CLI;
- stdin + stream-json + replacement system file;
- no setting sources/MCP servers, non-structured tools disallowed;
- no fallback model, session persistence, slash commands, Chrome, or prompt
  suggestions;
- safe/restricted mode enabled;
- guarded API-key/backend/retry environment variables removed.

No automatic cross-profile, cross-model, or cross-transport fallback exists.
Profile resolution must identify exactly one registered profile and verified
current conformance record.

## 21. Published headless contracts

These remain valid independent published contract executors:

```text
rb-harness headless init --output <absolute-isolated-root>
rb-harness headless interview run --state <absolute-state-root> ...
```

They are not the retired legacy semantic Init planner and are not aliases for
canonical semantic Init.

Relevant contract authorities:

- `contracts/rb-headless-init-v1.{md,schema.json}`;
- `contracts/rb-headless-interview-v1.{md,schema.json}`;
- packaged copies under `plugins/rb-harness/contracts/`.

Relevant runners:

- `packages/core/src/headless-contract.ts` and `headless-runner.ts`;
- `packages/core/src/headless-interview-contract.ts` and
  `headless-interview-runner.ts`;
- public stdin/EOF command wiring in `packages/core/src/cli-program.ts`.

Canonical `rb-harness init --headless` is different: it is the
non-interactive recommendation mode of canonical semantic Init, uses exact
profiles and the intent/work IR pipeline, and publishes the canonical three-
file `.rb` tree. It does not execute `rb-headless-init/v1`.

## 22. Package and CLI surface guard

The canonical CLI surface was rebaselined in
`packages/core/test/fixtures/cli-surface.json` and guarded by
`packages/core/test/cli-compatibility.test.ts`.

Current intended protections:

- canonical `rb-harness init` has no legacy compatibility waiver and promises
  only its new exact-profile options;
- retired planner options such as `--provider`, `--adapter`, `--output`,
  `--answers`, `--questions`, and `--first-output-timeout` are absent from
  canonical Init;
- published headless Init/interview entry points remain guarded;
- `rb-harness vnext init` is explicitly absent;
- `vnext conformance` remains a laboratory/experimental command and is omitted
  from the checked-in public fixture.

Observed discrepancy: `_provider-run` is intended to be an internal command,
but it is currently present in `cli-surface.json`. Because the compatibility
test iterates every fixture entry, `_provider-run` is today mechanically guarded
despite the stated intent not to add it to the public promise. This checkpoint
does not change the fixture. A later deliberate compatibility decision should
either document that promise or remove only that fixture entry with a focused
regression; do not infer a production behavior change from this debt.

Other relevant cutover guards:

- `packages/core/test/init-cutover.test.ts`;
- `packages/core/test/init-dashboard.test.ts`;
- `packages/core/test/vnext/providers/boundary.test.ts`;
- `packages/core/test/standalone.test.ts` and `headless-*.test.ts` for preserved
  independent contracts.

## 23. Generated plugin bundle

Bundle path:

```text
plugins/rb-harness/scripts/rb-harness.cjs
```

Current source-derived SHA-256:

```text
41a78d72bc689e1a18b64f912a2fa4cc29ebcc9b5cfeec7a239877355fe84ed2
```

Status:

- deterministically regenerated by `packages/core/scripts/build.mjs` from the
  current TypeScript source;
- modified relative to the Phase 3.5 freeze;
- must not be restored to the pre-cutover baseline;
- generated headless bundle manifests under `packages/core/dist/` pin the same
  current digest;
- two clean builds produced byte-identical bytes at this digest;
- historical digests must not override current source-to-artifact evidence.

Known packaging discrepancy: the standalone ESM package replays both
conformance records successfully, but invoking `vnext conformance` directly
through the generated CJS plugin bundle currently fails because esbuild replaces
`import.meta` in `defaultConformanceRecordsRoot()` with an empty value. Build
already warns about this. `node scripts/check-plugin.mjs` remains green because
it checks the bundled `--version`, not conformance replay. This checkpoint does
not fix the defect; it must not be confused with the planned recovery-budget
change.

## 24. Dashboard

Canonical Init uses the presentation-only dashboard in
`packages/core/src/harness-dashboard.ts`. `cli-program.ts` creates an
`InitDashboardController` and passes only an `onRunState` observer into
`runSemanticInit()`.

The visual composition includes:

- one RB HARNESS wordmark and Init/control-plane subtitle;
- canonical capybara mascot;
- project, workflow, current stage, status, and duration;
- canonical `pedido -> intent -> entrevista -> decisões -> work -> closure`
  pipeline;
- provider profile/transport/accounting panel;
- interview/semantic-operation/invocation/correction/provider-request/
  publication/token telemetry;
- bounded recent events;
- Ralph/final footer state (`Ralph READY` after publication).

The dashboard reads a safe orchestration snapshot only. It never receives the
original request, requirements, tasks, acceptance, credentials, or provider
envelopes. View derivation is pure and does not mutate the snapshot.

Presentation isolation is fail-closed with respect to execution:

- observer failures are swallowed in `runSemanticInit()`;
- renderer/output failures are swallowed by the controller;
- dashboard state cannot mutate semantic state;
- dashboard failure cannot change success, failure, counters, authority,
  closure, or publication.

Responsive behavior:

- panel layouts: wide at `>=120` columns, medium at `>=76`, narrow below `76`;
- full mascot remains until width drops below `68`, independent from panel
  breakpoint;
- output bounds width/height, truncates safely across ANSI/wide Unicode, and
  compresses events/provider/telemetry before critical execution state;
- short terminals retain dense provider and telemetry strips;
- non-TTY `--dashboard` warns and continues with textual logs.

Terminal lifecycle hides/restores the cursor, refreshes in place, pauses around
interactive questions, removes signal listeners, clears its timer, resets ANSI,
and restores the cursor on normal/failure/signal cleanup.

## 25. Capybara and product identity

The canonical mascot is implemented in
`packages/core/src/harness-mascot.ts`. It replaces the prior cow-like mascot and
uses a code-owned terminal pixel palette rendered with half-block cells.

Current dimensions:

- full/`wide`: source grid `30 x 14` pixels -> terminal footprint `30 x 7`;
- `compact`: source grid `17 x 8` pixels -> terminal footprint `17 x 4`.

It supports truecolor, monochrome Unicode, and ASCII degradation. The same
canonical mascot is composed into dashboard and splash. No semantic subsystem
imports or depends on mascot state. Do not regenerate/redesign the artwork in a
future task without an explicit visual request.

## 26. Dashboard manual acceptance

**CONFIRMED CONTINUATION CONTEXT:** the operator manually approved the current
terminal dashboard as good enough, although it is not pixel-identical to the
conceptual reference.

Recent real screenshots showed successful canonical Init, Ralph READY, the
complete dashboard, canonical pipeline, provider metadata, telemetry,
capybara, and completed publication. Pixel-perfect reproduction of the
reference mockup is not outstanding work. Dashboard visual remediation is
**DONE**.

Automated regression coverage is in
`packages/core/test/init-dashboard.test.ts` (22 passing tests in this
checkpoint baseline), including reference composition, breakpoints, state
projection, failure containment, safe metadata, ANSI geometry, mascot
dimensions, and cleanup.

## 27. Real dogfood evidence

Use this as empirical robustness context, not a formal statistical guarantee.

### Cron facility

The repository persists the natural Portuguese request with typos (`contrab`,
`abras`, etc.) and several Phase 3.5 evidence generations under
`packages/core/test/vnext/dogfood-evidence/`. The progression includes an early
successful greenfield run, precise failure evidence, and a later rule-ledger
success after bounded corrections. The operator additionally confirms repeated
successful real runs.

### Webhook service

**CONFIRMED CONTINUATION CONTEXT:** a persistence/idempotency/retry/queue-style
webhook service was exercised. First-pass hardening reduced recovery usage.
The complete raw run is not a committed canonical fixture here.

### Complex purchasing workflow

The request covered configurable approval tiers, concurrency,
rejection/correction, audit history, reapproval after value divergence, RBAC,
and UI/dashboard behavior.

**CONFIRMED CONTINUATION CONTEXT:** after the I-13 correction, three consecutive
runs reached:

```text
Ralph READY
semantic operations: 2
corrective regenerations: 0
```

The functional Portuguese UI acceptance extracted from this dogfood is now
committed as I-13 regression coverage.

Additional recent canonical-Init dashboard runs also succeeded, including one
run with one legitimate correction. This supports bounded recovery as model-
error tolerance; it does not prove a population-wide success rate.

## 28. Exact published `.rb` contract

Canonical Init publishes exactly:

```text
.rb/rb-manifest.json
.rb/init/BRIEF.md
.rb/init/PHASES.md
```

No other file enters the published Ralph artifact tree. Run state, staging,
previous-tree backup, provider/recovery/debug evidence, and rejected evidence
remain under `.rb-harness/runs/<runId>/`.

Publication semantics:

- `BRIEF.md` is a non-executable project brief containing objective,
  determinations/defaults, requirements, protected paths, and quality kinds;
- `PHASES.md` is the one `rb-execution/v1` READY execution plan;
- the manifest contains exactly two artifact records (BRIEF and PHASES), with
  hashes computed from exact staged bytes;
- manifest schema and tree validation close before publication;
- PHASES parse/render round-trip must preserve execution semantics;
- exactly one READY execution plan must match the PHASES artifact marker;
- published bytes are rehashed after publication.

No `OPERATIONS.json`, free-form extra documentation, run state, or repair report
is published by canonical Init.

## 29. Progressive Init Phase 1 foundation and Phase 2 user stories

**PHASE 1 FROZEN / PHASE 2 FINAL-FREEZE READY AND CURRENTLY UNCOMMITTED**

Progressive Init Phase 1 is frozen at
`bb90ab81d9d3580c571035e3e36399ad458b0c4a` on
`feat/progressive-init-foundation`. Phase 2 is isolated on
`feat/progressive-init-user-stories` from exactly that commit.

The code-owned closed vocabulary and canonical order are:

1. `project-description`;
2. `user-stories`;
3. `database-schema`;
4. `project-phases`.

Implemented Phase 1 execution contract:

- the automatic coordinator selects the earliest incomplete or stale stage and
  remains directly testable internally, but does not yet own bare public Init;
- `rb-harness init`, `rb-harness init <request>`, profile-bearing bare Init,
  `rb-harness --init`, and root-wizard Init continue through canonical
  `intent -> interview -> work -> closeInitProject() -> .rb` publication;
- `rb-harness init --stage <stage>` runs only the selected stage after strict
  prerequisite classification and never advances automatically; the supported
  root-selector equivalent is `rb-harness --init --stage <stage>`;
- `--stage ... --dashboard` fails before semantic execution because the
  canonical intent/work dashboard does not represent Progressive stage state;
- stage presentation and interview context identify the active stage before
  semantic questions;
- `.spec/init/project-description.md` is the developer-owned semantic source,
  parsed and rendered through the strict `rb-project-description/v1` contract;
- bounded secret-safe repository discovery and accepted stage decisions feed a
  deterministic authoritative-input freshness hash;
- valid developer edits are reparsed as current typed authority; concurrent or
  structurally ambiguous edits fail closed;
- provider/model output cannot author `developer` authority under any
  materiality or rigidity; the decoder rejects that source even if provider
  schema enforcement is bypassed;
- genuine live request, user-answer, and accepted-recommendation authority is
  independently verified by Core before the semantic result is accepted;
- manual dogfood exposed that resolved project-description questions could be
  silently omitted when the provider did not reference them as determinations;
  every verified material project-description interview decision is now
  materialized by Core, with its key, selected statement, materiality,
  rigidity, rationale, and source owned by the verified question/answer
  evidence rather than re-authored or optionally selected by the provider;
- provider-authored project-description determinations are limited to verified
  request facts and permitted model defaults; provider/question and
  developer/question key collisions fail closed rather than overwriting either
  authority;
- for live request authority, the model selects a meaningful contiguous request
  span and Core derives the authority-bearing request fact from that verified
  evidence; the wire contains no second model-authored request statement and
  Core uses no lexical-overlap or natural-language entailment heuristic;
- persistence truthfully converts every accepted determination to `developer`
  authority; the strict Markdown parser accepts no historical live source
  labels, while valid developer edits remain typed semantic authority;
- every relevant accepted determination contributes its semantic content—not
  a historical authority label—to the canonical freshness projection;
- `.spec/init` is protected developer-owned state: canonical and future
  Progressive executable plans cannot own that path, a child, or an
  intersecting bounded glob, while unrelated `.spec` paths remain allowed;
- `.rb-harness/progressive-init` contains status/audit metadata only and is not
  read back as semantic authority;
- Phase 1 never publishes to or changes `.rb`;
- project-description correction regenerates one complete stage candidate and
  is isolated from canonical `intent|work` recovery accounting.

Focused Progressive profile choice is explicit at the semantic-execution
boundary. When `--profile` is supplied, that exact supported profile controls
model, provider/agent, transport, and request-accounting behavior and no
selector or fallback is used. When an interactive TTY omits `--profile`, the
operator selects from the exact currently usable `SUPPORTED` registry profiles
before stage presentation or provider execution; the selected profile ID,
transport, and request-accounting mode are displayed. Headless or non-TTY
execution that needs semantic work fails before provider execution unless an
exact `--profile` is supplied. A sole usable supported profile may be selected
directly but is still displayed. Profile selection and credential resolution
remain separate. This does not modify canonical bare Init profile/wizard
behavior, and it does not resolve the separately known direct-API Progressive
response-envelope defect.

Phase 2 implements the separate strict `rb-user-stories/v1` semantic stage:

- `.spec/init/user-stories.md` is the developer-owned semantic source;
- every story has a stable SemanticKey plus a persisted Core-allocated
  developer-facing `US-x.y` identity;
- every story explicitly names one workflow and one or more capabilities it
  realizes; workflow membership never implies capability coverage, every
  workflow needs at least one story, and every approved capability must appear
  in at least one story whose workflow owns that capability;
- `Actor` is the business/story actor and `Operator` is the actor that directly
  operates or interacts with the system; both are required, explicit, and
  bounded by the upstream workflow actor set;
- Actor and Operator must be equal by default. A mediated pair is valid only
  when an accepted Core-owned `capability-participation` structural decision
  binds the exact workflow, capability, Actor, and Operator;
- Core deterministically enumerates one Capability Participation subject for
  every upstream `(workflowKey, capabilityKey)` pair before question selection.
  The same capability in two workflows creates two independent authority
  subjects, and a persisted decision settles only its exact pair;
- a workflow with one actor needs no participation question or structural
  decision: `Actor = Operator = workflow.actorKeys[0]` is a deterministic
  invariant, not synthetic developer authority. Every unresolved subject whose
  workflow has multiple actors requires explicit Core-owned structural
  resolution;
- Core owns participation-question detection, subject identity, question key,
  option keys, and Actor/Operator pairs. The provider returns exactly one
  recommendation/presentation record for every required Core subject and may
  supply wording and rationale, but it cannot classify prose as structural,
  omit or invent a subject, merge workflow-scoped subjects, or construct typed
  options;
- participation Step 1 contains the workflow actors' equality pairs plus one
  deterministic escape option. Ordinals and exact Core option keys are the only
  accepted structural answers; blank accepts only a valid Step-1 provider
  recommendation, and arbitrary prose cannot manufacture keys;
- the escape opens a local, provider-free Step 2 containing the complete
  deterministic Cartesian product of globally declared actors on both the
  Actor and Operator axes. This option universe is not capped at eight. Step 2
  accepts only an ordinal or exact Core pair key and has no inferred
  recommendation;
- globally known participants remain selectable in Step 2 even when absent
  from the current workflow. Such a selection intentionally reaches
  `USER_STORIES_UPSTREAM_REFINEMENT_REQUIRED` before candidate generation,
  without upstream mutation or fallback;
- Core materializes both the human-readable interview determination and the
  typed structural decision under the same SemanticKey. Every structural
  decision requires exactly one matching determination, while ordinary prose
  determinations need no structural decision;
- every story claiming a structurally bound capability must use that exact
  Actor/Operator pair. Multi-capability stories must satisfy every claimed
  capability simultaneously and fail deterministically when those
  participation requirements conflict;
- after persistence, structural decisions and stories are developer-owned
  semantics. The provider candidate authors stories only and cannot author,
  delete, or mutate structural decisions, determinations, or stable IDs;
- a newly accepted structural decision that selects a globally known Actor or
  Operator outside the upstream workflow fails before candidate generation as
  `USER_STORIES_UPSTREAM_REFINEMENT_REQUIRED`; Core neither expands the
  Project Description nor substitutes another participant;
- the capability-to-workflow readiness check remains a prerequisite before any
  user-stories semantic operation;
- the stage consumes a narrow canonical project-description projection, not
  the raw request, repository discovery, quality commands, or provenance
  labels;
- Core-owned material structural questions are recommended by the single
  question-selection operation and resolved locally before the complete story
  candidate is authored. Healthy execution remains exactly two semantic
  operations; the escape follow-up adds none;
- live dogfood exposed that asking the candidate to restate verified interview
  determinations created an unsatisfiable duplicate-authority boundary for
  natural model paraphrases;
- Core now verifies each selected interview decision and directly materializes
  its authority-bearing determination: key and statement come from the verified
  question decision, materiality and rigidity come from the question, and
  source and rationale come from verified interview/recommendation evidence;
- the story candidate receives those resolved decisions only as structural
  constraints and authors stories, not determinations; candidate prose may
  naturally express a decision without duplicating its selected value verbatim;
- existing persisted developer determinations remain Core-owned input, and a
  new question whose key collides with one fails before candidate authoring;
- the complete candidate receives at most one whole-stage corrective
  regeneration, without re-asking questions or changing canonical recovery;
- provider output contains neither determinations nor `storyId`; consequently it
  cannot author user-answer, accepted-recommendation, question, materiality,
  rigidity, acceptance-mode, or developer authority;
- accepted live determinations become developer authority at the strict
  persistence boundary;
- no user-stories allocation or semantic authority is stored under
  `.rb-harness`; status and freshness are recomputed from the strict `.spec`
  sources and deterministic inputs;
- a structurally valid developer-owned user-stories source whose symbolic
  references are invalidated by a fresh project-description is classified as
  `reconciliation-required`, with deterministic reference findings;
- reconciliation findings include removed or moved capability references,
  Actor or Operator eligibility loss, and incompatible persisted structural
  participation decisions; intrinsic Markdown/allocation corruption remains a
  distinct document-invalid failure;
- reconciliation-required never mutates or repairs developer semantics:
  upstream stages remain reachable, while focused/automatic user-stories stop
  before provider execution until the developer explicitly reconciles the
  strict `.spec/init/user-stories.md` source;
- additive upstream changes that leave existing story references compatible
  remain ordinary stale state and may be extended under the existing provider
  preservation rules; destructive reconciliation/supersession policy remains
  deliberately unimplemented;
- the project-description and user-stories stores share the same narrow
  symlink, containment, expected-hash, concurrent-edit, exclusive-temporary,
  and atomic-replacement mechanics;
- focused `--stage user-stories` completes only user-stories, while internal
  automatic progression identifies `database-schema` next and stops.

Progressive stage execution is freshness-driven at the shared coordinator
boundary. Live dogfood exposed that explicitly selecting an already fresh User
Stories stage incorrectly reopened question selection and candidate authoring,
creating conflicts with valid developer-owned stories. The corrected Phase-2
contract is:

- `incomplete` and `complete-stale` stages execute their normal semantic
  operation;
- `reconciliation-required` remains an actionable developer reconciliation
  boundary with no provider call;
- a `complete-fresh` developer-owned stage is an idempotent success with zero
  semantic operations, corrections, provider calls, interview questions, or
  writes, including when explicitly selected through `--stage`;
- explicit stage selection chooses the stage boundary; it does not authorize
  destructive revision of fresh developer-owned semantics;
- `developerModified` is not a regeneration trigger when the strict stage
  source remains complete-fresh;
- any future typed authority category must be Core-owned from inception;
  provider classification alone is insufficient;
- any future force, revision, or supersession capability requires its own
  explicit authority contract and is not part of Phase 2.

The final terminal interview presentation renders prose alternatives one per
numbered line and structural choices one pair per numbered block. Internal
Core option keys remain valid exact-key inputs but are not primary visible
labels. Interactive structural selections re-prompt locally after invalid
input, with a maximum of three invalid attempts: Step 1 accepts blank only when
there is a valid recommendation, while Step 2 never accepts blank. These local
retries and the Step-2 escape consume no semantic operation, corrective
regeneration, or provider call. Headless selection remains fail-closed. Screen
clearing, TTY redraw, and dashboard interview redraw are intentionally deferred
until the later Progressive presentation layer; the canonical dashboard and
capybara remain untouched.

Concise Phase-2 manual dogfood evidence:

- runtime selection resolved `fable` to `claude-fable-5`, required explicit
  compatibility verification before SUPPORT, and reused that evidence in a
  later process;
- Project Description interview answers persisted correctly, and its fresh
  rerun consumed zero semantic operations;
- initial User Stories dogfood exposed and drove bounded remediation of false
  workflow-derived capability coverage, collapsed business Actor/system
  Operator semantics, provider-controlled structural-question classification,
  and cross-workflow participation ambiguity;
- final User Stories dogfood proved Core-owned workflow/capability subjects,
  explicit Actor/Operator selection including mediated `cliente / atendente`,
  the pre-candidate `USER_STORIES_UPSTREAM_REFINEMENT_REQUIRED` boundary,
  direct Project Description refinement followed by a successful rerun,
  successful candidate generation with two semantic operations and zero
  corrective regenerations, direct developer prose/acceptance cleanup, and a
  final complete-fresh rerun with zero semantic operations.

`database-schema` (including not-applicable), `project-phases`, and
Progressive-to-canonical Ralph projection remain **PLANNED / NOT
IMPLEMENTED**. No placeholder database or phase artifact exists. The canonical
Ralph closure described in section 28 remains untouched.

This is a temporary integration boundary, not the final approved UX. After all
four Progressive stages and final Ralph convergence are implemented, a later
explicit cutover will make bare `rb-harness init` invoke automatic Progressive
continuation at the earliest incomplete/stale stage. No hidden public automatic
mode flag is introduced during Progressive construction.

Do not implement any Progressive Init element as part of recovery-budget work.

## 30. Reviewed external progressive-init inspiration

**CONFIRMED CONTINUATION CONTEXT:** the four reviewed command specifications
contributed these useful ideas:

- project description: discover environment first;
- user stories: prove workflow-to-story coverage;
- database schema: prove concept-to-table coverage;
- project phases: prove story/table-to-task coverage;
- deterministic input stamps and freshness;
- incremental reruns that preserve developer decisions;
- stable IDs and numbering.

Policies that must not be copied blindly:

- universal lookup tables for every category;
- mandatory database-first phase ordering;
- framework-specific opinions promoted to universal laws;
- direct free-form Markdown editing treated as semantic authority.

RB Harness must preserve typed workflow IR, deterministic validation,
deterministic rendering, and exact closure.

## 31. Provider execution identity and future expansion

**Claude Code runtime-model selection is implemented in the current uncommitted
Phase 2 work. Other provider expansion remains PLANNED / NOT IMPLEMENTED.**

Claude Code now separates these operational identities:

```text
transport profile = anthropic:claude-code-cli
requested model   = exact selector or moving alias
resolved model    = concrete identity observed in every model-bearing stream field
compatibility     = packaged bootstrap evidence or user-level runtime evidence
```

The exact legacy profile
`anthropic:claude-code-cli:claude-opus-5` remains a resolver alias for
`requestedModel=claude-opus-5`; it does not convert to the moving `opus` alias.
The packaged 26/26 record bootstraps only that exact recorded tuple. Availability
does not imply SUPPORT: aliases such as `opus`, `sonnet`, `fable`, and `haiku`,
and custom future selectors, remain UNVERIFIED until an operator explicitly
runs the complete existing Claude Code conformance contract.

Runtime compatibility evidence is integrity-bound operational state under the
XDG user-state root:

```text
$XDG_STATE_HOME/rb-harness/provider-compatibility/
```

with the platform fallback when `XDG_STATE_HOME` is absent. It binds the exact
Claude Code version, invocation-policy digest, requested selector, observed
concrete model, conformance contract/capability envelope, opaque accounting,
and complete sanitized conformance record. It is not project semantic authority
and is never stored in `.spec`, `.rb`, or project `.rb-harness`.

Interactive focused Progressive execution selects transport first and then a
Claude Code model. The UI distinguishes SUPPORTED, UNVERIFIED, STALE,
UNAVAILABLE, and UNSUPPORTED. Verification makes bounded real model calls and
therefore requires explicit consent. Headless/non-TTY dynamic execution uses:

```text
--profile anthropic:claude-code-cli --model <selector>
```

and requires current evidence created explicitly through the internal lab:

```text
rb-harness vnext conformance anthropic:claude-code-cli \
  --verify-runtime-model <selector>
```

No fallback is configured. If a cached alias later resolves to another concrete
model, or Claude Code's exact version changes, semantic output is rejected
before Core acceptance and explicit re-verification is required. A
`complete-fresh` Progressive stage returns before any provider/model selection
or compatibility lookup.

The current Anthropic direct-API exact profile remains unchanged. Dynamic direct
API discovery is **NOT IMPLEMENTED**, and the separate known Progressive direct-
API `{input: ...}` live-envelope defect is not resolved by this work. Canonical
Init retains its existing exact-profile CLI/wizard topology; the shared provider
adapter can consume verified runtime targets, but canonical dynamic-selection UI
is deliberately deferred.

Likely later provider order remains Codex CLI, OpenAI API, and OpenCode CLI.
Every new provider/transport still requires a semantic-blind adapter, attributable
compatibility evidence, fail-closed SUPPORT classification, and no automatic
fallback. CLI subscription transports may use `requestAccounting: opaque` when
provider-internal requests cannot be authoritatively measured; Harness-owned
transport invocations remain exactly counted.

## 32. Legacy cleanup status

“Legacy is dead” means the following ownership is removed/retired:

- the legacy semantic Init planner no longer owns public Init;
- the old public canonical Init route/options are rebaselined away;
- public `vnext init` is removed;
- old standalone Init runs cannot be resumed through the generic resume path.

The following remain intentionally preserved:

- published headless contract executors;
- credential management/vault integration;
- provider infrastructure used by legitimate workflows;
- artifact verification and manifest/tree operations;
- shared terminal/dashboard/splash primitives;
- `ai-context`, `plan`, `evolve`, and `review` implementations;
- historical validation contracts still consumed by those workflows;
- conformance laboratory commands/records.

Do not perform broad dead-code deletion without reachability and published-
contract inspection. The headless-contract preservation incident demonstrates
why matching the word “init” is not proof of legacy ownership.

## 33. Current local installation

Observed user-local executable:

```text
/home/bruno/.local/bin/rb-harness
  -> ../lib/node_modules/@rb-harness/core/dist/cli.js
  -> /home/bruno/.local/lib/node_modules/@rb-harness/core/dist/cli.js
```

- It is a symbolic link (`lrwxrwxrwx`, link mode shown as `777`).
- Package location:
  `/home/bruno/.local/lib/node_modules/@rb-harness/core`.
- Installed package version: `1.0.0`.
- Executable `--version`: `1.0.0`.
- `command -v rb-harness` resolves exactly to
  `/home/bruno/.local/bin/rb-harness`.

This user-local installation is distinct from the resolved, uncommitted
cutover source, whose package/plugin/runtime version is `0.6.2`. It was not
reinstalled or used to redefine the source version.

No reinstall and no shell-configuration change was performed.

## 34. Verification baseline

All verification was safe/offline. Live-test variables were explicitly unset;
no `--record` command was used.

| Verification | Result |
|---|---|
| `npm run build` twice | PASS; consecutive bundle SHA-256 values both `03a98f197f30a08f3e57cd3c7ef5731d2e5e427c384e4e1d5837eb8e3869b366`; one pre-existing esbuild warning about `import.meta` in CJS |
| `npm run check` | PASS end to end: build, typecheck, standalone package, full safe suite, Bash compatibility, and plugin check |
| full safe suite `npm test` | PASS: 59 test files passed, 3 skipped; 947 tests passed, 3 skipped (950 total) |
| `npm run typecheck` | PASS (`tsc --noEmit`) |
| `npm run check:package` | PASS; packed `rb-harness-core-0.6.2.tgz` and ran every workflow through an installed bin symlink |
| `node scripts/check-plugin.mjs` | PASS; 69 plugin files internally consistent |
| `bash scripts/test-bash-compat.sh` | PASS; Bash resolved the READY plan from the manifest tree |
| final Phase-2 focused freeze suite | PASS: 11 files, 200 tests; Progressive Phases 1/2, profile/CLI boundaries, runtime compatibility, packaged conformance, canonical recovery/cutover, and CLI compatibility |
| canonical dashboard test | PASS: 1 file, 22 tests |
| headless bundle/init/interview tests | PASS: 3 files, 13 tests |
| offline direct-API conformance replay | PASS: SUPPORTED, 16/16 |
| offline Claude Code CLI conformance replay | PASS: SUPPORTED, 26/26 |
| `git diff --check` | PASS after checkpoint write |

The three skipped tests are live/recording gates and were intentionally not
enabled. Fixture adapters/processes were exercised by tests, but external
provider/model calls were **0**.

### 34.1 Final blocker diagnosis and resolution

The published-headless-contract test `rejects prompt-bearing instructions
before the distributed adapter executes` retains its complete 8-by-16
phrase/field matrix, unsupported-generation-scope assertions, and proof that
the distributed adapter is absent. Product behavior was not changed. The
original failure was a process-timing margin issue: the test launches 128
short-lived generated-CLI processes, and its outer 20-second test budget became
tight under concurrent full-suite load after the cutover bundle grew. It passed
current isolated/full-suite and freeze isolated/full-suite reruns. The only
stabilization was to raise that outer test timeout from 20 to 60 seconds; every
individual process deadline and all assertions remain unchanged. The focused
three-file headless gate then passed 13/13 in 15.62 seconds, and the full suite
also passed.

The incremental-generation test `formats a malformed legacy part envelope
without regenerating its semantic content` was not changed. Its relevant
production source is blob-identical to the Phase 3.5 freeze, and the initial
failure could not be reproduced in subsequent isolated, file, or full-suite
runs. The exact selected regression passed in the final verification. If it
fails again, capture its exact structured assertion/exception before changing
production or test code.

### 34.2 Version-family ownership resolution

The incomplete `1.0.0` sweep is not part of this cutover. The following files
were restored wholly to HEAD because their only meaningful diff was the version
sweep: `.claude-plugin/marketplace.json`, `README.md`, `README.pt-BR.md`,
`packages/core/package.json`, `packages/core/src/version.ts`,
`plugins/rb-harness/.claude-plugin/plugin.json`, and
`plugins/rb-harness/.codex-plugin/plugin.json`. In the mixed
`docs/funcionalidades.pt-BR.md` diff, only two version references were restored
to `0.6.2`; its canonical-Init documentation remains. The unrelated root
`package.json` and `AGENTS.md` were not touched and must stay outside the
cutover commit.

Important packaging exception: the ESM source/dist/packed standalone replay is
green. Direct replay through `plugins/rb-harness/scripts/rb-harness.cjs` fails
for both profiles with:

```text
ERROR: The "path" argument must be of type string or an instance of URL. Received undefined
```

This is the CJS `import.meta` issue described in section 23. It does not make
the green `npm run check` result false; it identifies a gap not covered by that
check.

## 35. Known boundaries and do-not-do list

For canonical Init:

- no automatic provider/model/transport fallback;
- no semantic formatter;
- no JSON fixer;
- no Markdown repair;
- no document splicing;
- no partial candidate patching;
- no post-publication semantic repair;
- no silent RIGID model defaults for product/architecture decisions;
- no universal `ProjectModel`;
- no arbitrary `parallelSafe` inference (`false` is Core-owned);
- no unsupported `OPERATIONS.json` publication;
- no accidental legacy Init reintroduction;
- no public recovery-budget knob;
- no dashboard/mascot authority over semantics;
- no raw provider envelopes/secrets in run-state evidence;
- no provider-specific semantic prompt/repair policy in adapters.

These boundaries are scoped to canonical semantic Init. Other preserved
standalone workflows still contain their own historical document-generation/
structural-correction machinery and must not be conflated with canonical Init.

## NEXT SESSION START HERE

1. Read this checkpoint first.
2. Treat it as authoritative for current post-Phase-3.5/cutover state.
3. The canonical Init cutover is committed at
   `744305e52012462770b2a9815fc8821a64102954`, whose Phase 3.5 parent is
   `0bbe610f623c6417d30ed2d57f019bb95dba2404`.
4. Do **not** repeat completed dogfood remediation or redesign the dashboard or
   capybara.
5. The bounded recovery-tolerance increase is frozen and pushed at
   `5a3481ee74f00c8a7e1805f49330c1220803505a`. Its production envelope is:

   - corrections per slice: `2`;
   - corrections per run: `3`;
   - semantic operations: `5`;
   - transport invocations: `7`;
   - transport retry per operation remains `1`;
   - transport retries per run remains `2`.

   Keep `package.json` and `AGENTS.md` outside this work.
6. Preserve whole-slice regeneration, global rule application, authoritative
   input hashes, provider neutrality, retry independence, and fail-closed
   publication in all continuation work.
7. Keep the CJS conformance-replay gap
   and `_provider-run` fixture discrepancy separate unless the operator expands
   scope.
8. Progressive Init Phase 1 is frozen at
   `bb90ab81d9d3580c571035e3e36399ad458b0c4a`. Phase 2 user-stories is
   implemented but uncommitted on `feat/progressive-init-user-stories` from
   exactly that foundation. Preserve the internal automatic coordinator,
   public explicit focused `--stage` route, canonical bare-Init bridge, strict
   protected developer-owned `.spec/init` sources, stable story identities,
   and explicit not-implemented boundary for database-schema/project-phases.
   The approved future cutover remains automatic Progressive bare Init after
   all four stages and Ralph convergence are ready.
9. Preserve the Claude Code transport/runtime-model split and user-level
   compatibility evidence. Keep dynamic direct-API discovery and other provider
   expansion as separate work.

The exact next action is: **commit the independently reviewed Progressive Init
Phase 2 work as one bounded unit when authorized.** The final freeze is green,
but no Phase 2 implementation commit hash exists yet.
